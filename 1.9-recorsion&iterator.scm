;; 1.9

(define (sum a b)
  (if (= a 0)
	b
	(inc (sum (dec a) b))))
;; 递归
;; 1 + (+ 3 5)
;; 1 + 1 + (+ 2 5)
;; 1 + 1 + 1 + (+ 1 5)
;; 1 + 1 + 1 + 1 + (+ 0 5)
;; ...


(define (sum a b)
  (if (= a 0)
	b
	(sum (dec a) (inc b))))
;;迭代
;; (+ 3 6)
;; (+ 2 7)
;; (+ 1 8)
;; (+ 0 9)

